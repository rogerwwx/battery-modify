[package]
name = "battery_calibrate"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4.34", default-features = false, features = ["local"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = "debuginfo"
incremental = false
